#pragma once 
#include "Vector.h"
#include "Vector.cpp"

template class Vector<int>; // явно создаем экземпляр шаблона класса Vector<int>
template class Vector<long double>; // явно создаем экземпляр шаблона класса Vector<long double>